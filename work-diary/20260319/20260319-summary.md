# 2026-03-19 工作日记

## 主题

Connect — 安全加固 + 游戏体验 + 任务系统重构 + 品牌建设 + 宣传视频策划

## 完成内容

### 1. 安全加固 & 数据一致性

- API Key 改用 hash 存储，新增 Prisma migration
- 全面修复类型错误（TypeScript strict 兼容）
- 删除废弃的 `/api/auth/complete` 路由
- `credits.ts`、`auth.ts`、`secondme.ts`、`store.ts` 等多处安全改进
- `ImageCarousel` 组件修复

### 2. Vercel 部署兼容

- Logger 跳过文件写入（适配 serverless 环境）
- Navbar credits 实时刷新

### 3. 游戏系统优化

- AI 思考气泡 UI + NPC 人格 system prompt
- Blackjack / Texas Hold'em 逻辑改进
- 游戏观战体验优化：事件队列播放 + thinking 气泡修复 + 真人分身决策
- pitfalls 文档补充

### 4. 广场 × 咨询系统集成

- 发帖自动匹配分身 + 精美卡片展示
- 新增 `/api/v1/plaza/[id]/consult` 咨询路由
- ConnectPanel 联动更新

### 5. Landing Page 改版

- 功能卡片改为 16:9 真实图片轮播 + 自动滚动
- 品牌标语改为「连接对的人，帮你做对的事」

### 6. 绘画任务升级

- 改用 SiliconFlow Kolors 生图
- System prompt 引导 + 错误处理
- Credit 消耗降为 1

### 7. 任务系统大重构

- 统一广场展示逻辑
- SSE 实时推送（新增 `/api/v1/tasks/[id]/stream`）
- 多轮对话支持
- BM25 fallback 搜索
- 新增 `TaskCreatingOverlay` 组件 + `task-events.ts` 事件模块

### 8. 品牌定位

- 新建 `brand/` 目录，撰写品牌定位文档（核心理念、使用场景、差异化）

### 9. 宣传视频策划

- 新建 `promo/` 目录
- 六幕创意脚本：用户发起 → 信号流转 → 协同办公 → 结果反馈 → 同事翻车 → 牌桌揭秘
- AI 视频生成工具调研（可灵 / Vidu / Runway / Sora 等）

## 今日 Commits（11 个）

| Commit | 内容 |
|--------|------|
| `35b18ad` | feat: AI 思考气泡 + NPC 人格 system prompt + pitfalls 文档补充 |
| `8672290` | fix: 安全加固 + 数据一致性 + 类型修复 |
| `d8da5f5` | fix: Vercel 部署兼容 — logger 跳过文件写入 + Navbar credits 实时刷新 |
| `8b8f4c0` | feat: 广场×咨询系统集成 — 发帖自动匹配 + 精美卡片 + 手动/自动咨询 |
| `8930f7e` | fix: 游戏观战体验优化 — 事件队列播放 + thinking 气泡修复 + 真人分身决策 |
| `6004672` | feat: Landing Page 功能卡片改为图片轮播 — 16:9 真实图片 + 自动滚动 |
| `fe0b596` | feat: 绘画任务改用 SiliconFlow Kolors 生图 — system prompt 引导 + 错误处理 + credit 降为 1 |
| `29135f0` | update: 品牌标语改为「连接对的人，帮你做对的事」 |
| `f35b4be` | feat: 任务系统大重构 — 统一广场展示 + SSE 实时推送 + 多轮对话 + BM25 fallback |
| `5634e12` | docs: 新建 brand/ 品牌定位文档 — 核心理念、场景、差异化 |
| `8701e8f` | docs: 宣传视频创意脚本 & AI视频工具调研 |

## 待办

- [ ] 宣传视频：用 Midjourney/FLUX 生成各场景关键帧
- [ ] 宣传视频：选定工具试做第一个片段
- [ ] 向量搜索优化（当前已有 BM25 fallback）
